server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta de React Router que no matchee un archivo cae a index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
